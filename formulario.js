var datosValidacion = [];
var nombre = toLowerCase(prompt=("Ingresa nombre completo"));
var telefono = parseInt(prompt("Ingresa tu numero celular a 10 digitos"));
var email = prompt("Ingresa tu correo electronico");
var contra = prompt("Agrega una contraseña");
  var datosCompletos = {
    nombre: nombre,
    telefono: telefono,
    email: email,
    contra: contra,

//for(i=0; i<datosCompletos.length ;i++){

  //}

    //datosValidacion.push(datosCompletos);
    //document.write(datosValidacion)
}
